<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <!--   <item name="android:windowBackground">@android:color</item>-->
        <item name="buttonStyle">@style/Widget.AppCompat.Button.Small</item>
        <item name="android:windowIsTranslucent">false</item>
    </style>

    <!-- 主页主题, 不支持侧滑背景为白色，有title-->
    <style name="MainTheme" parent="AppTheme">
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:windowIsTranslucent">false</item>
    </style>

    <!-- 全部填充屏幕, 背景为白色-->
    <style name="FullTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowBackground">@android:color/white</item>
    </style>
    <!---->
    <style name="SplashTheme" parent="FullTheme">
        <item name="android:background">@drawable/splash</item>
    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

</resources>
